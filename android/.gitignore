# Gradle
.gradle/
**/build/

# Local SDK path (machine-specific — never commit)
local.properties

# Native .so libs — build artifacts, regenerated by build-android.sh
# from target/<triple>/debug|release/. Committed source is in ../src.
app/src/main/jniLibs/**/*.so
